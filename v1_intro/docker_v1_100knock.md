# Docker慎重入門100本ノック — v1 入門編

## 位置づけ

Dockerを怖がらず、容量・コンテナ・イメージを見ながら安全に扱う。

- v1入門編: まず壊さず、意味を確認しながら手を動かす。
- v2応用・実践編: 研究実務で使う形に近づける。

## 使い方

1. まず各Knockのコマンドを読む。
2. 練習用ディレクトリで実行する。
3. 解説と確認ポイントを見て、何が起きたか確認する。

注意: `myserver`, `your_username`, `git@example.com:USER/REPO.git` などは仮の名前です。実環境に合わせて置き換えてください。

## Knock 001: Dockerがあるか確認する

**目的**: Docker利用可否を確認する。

````bash
command -v docker || echo "docker not installed"
````
**解説**: 会社PCではインストールや起動権限が制限されることがあります。

**確認ポイント**: パスまたは未導入表示。


## Knock 002: Dockerバージョンを見る

**目的**: Docker CLIのバージョンを見る。

````bash
docker --version
````
**解説**: まずCLIが動くか確認します。

**確認ポイント**: Docker versionが出る。


## Knock 003: Docker daemon疎通確認

**目的**: Docker本体が起動しているか確認する。

````bash
docker info | head -n 20
````
**解説**: CLIがあってもdaemonが止まっていると使えません。

**確認ポイント**: 情報または接続エラーが出る。


## Knock 004: イメージ一覧を見る

**目的**: ローカルのDockerイメージを見る。

````bash
docker images
````
**解説**: イメージはディスク容量を食います。

**確認ポイント**: 一覧が出る。


## Knock 005: コンテナ一覧を見る

**目的**: 動いているコンテナを見る。

````bash
docker ps
````
**解説**: メモリやCPUを使うのは主に実行中コンテナです。

**確認ポイント**: 一覧が出る。


## Knock 006: 停止中も含めて見る

**目的**: 過去に作ったコンテナも見る。

````bash
docker ps -a
````
**解説**: 停止コンテナもディスクを使います。

**確認ポイント**: 全コンテナ一覧が出る。


## Knock 007: 容量を見る

**目的**: Dockerが使う容量を見る。

````bash
docker system df
````
**解説**: Dockerへの苦手意識があるなら最重要コマンドです。

**確認ポイント**: Images/Containers/Volumesの容量が出る。


## Knock 008: hello-worldを実行する

**目的**: 最小コンテナを実行する。

````bash
docker run --rm hello-world
````
**解説**: `--rm` により終了後にコンテナを残しません。初心者には安全です。

**確認ポイント**: Hello from Dockerが出る。


## Knock 009: ubuntuを一回だけ実行する

**目的**: Ubuntuコンテナを実行する。

````bash
docker run --rm ubuntu:22.04 cat /etc/os-release
````
**解説**: 手元OSと別のLinuxユーザ空間を一時的に動かせます。

**確認ポイント**: Ubuntu情報が出る。


## Knock 010: コンテナ内でpwdする

**目的**: コンテナ内の現在地を見る。

````bash
docker run --rm ubuntu:22.04 pwd
````
**解説**: コンテナ内はホストとは別のファイルシステムです。

**確認ポイント**: `/` が出る。


## Knock 011: コンテナ内でlsする

**目的**: コンテナ内のルートを見る。

````bash
docker run --rm ubuntu:22.04 ls -lah /
````
**解説**: ホストPCのファイルがそのまま見えるわけではありません。

**確認ポイント**: Linuxディレクトリが出る。


## Knock 012: 名前付きコンテナを起動する

**目的**: バックグラウンドコンテナを起動する。

````bash
docker run -d --name ubuntu100_sleep ubuntu:22.04 sleep 300
````
**解説**: `-d` はdetach、`--name` は名前指定です。

**確認ポイント**: docker psに出る。


## Knock 013: コンテナログを見る

**目的**: コンテナの標準出力を見る。

````bash
docker logs ubuntu100_sleep
````
**解説**: サーバプロセスの確認に使います。

**確認ポイント**: この例ではほぼ空。


## Knock 014: コンテナ内でコマンド実行

**目的**: 動作中コンテナに入らずコマンド実行する。

````bash
docker exec ubuntu100_sleep cat /etc/os-release
````
**解説**: `docker exec` はデバッグでよく使います。

**確認ポイント**: Ubuntu情報が出る。


## Knock 015: コンテナを停止する

**目的**: 実行中コンテナを止める。

````bash
docker stop ubuntu100_sleep
````
**解説**: 使わないコンテナは止めるとメモリを使いません。

**確認ポイント**: コンテナ名が表示される。


## Knock 016: 停止コンテナを削除する

**目的**: 停止コンテナを削除する。

````bash
docker rm ubuntu100_sleep
````
**解説**: 停止コンテナは容量を使うので掃除対象です。

**確認ポイント**: 名前が表示される。


## Knock 017: イメージを確認する

**目的**: ubuntuイメージが残っているか確認する。

````bash
docker images ubuntu
````
**解説**: コンテナを消してもイメージは残ります。

**確認ポイント**: ubuntu行が出る。


## Knock 018: docker runオプションをメモ: --rm

**目的**: よく使うrunオプションを覚える。

````bash
echo "--rm" >> docker_run_options.txt && tail -n 1 docker_run_options.txt
````
**解説**: Dockerはオプションの意味が分かると怖さが減ります。

**確認ポイント**: メモに追記される。


## Knock 019: docker runオプションをメモ: -it

**目的**: よく使うrunオプションを覚える。

````bash
echo "-it" >> docker_run_options.txt && tail -n 1 docker_run_options.txt
````
**解説**: Dockerはオプションの意味が分かると怖さが減ります。

**確認ポイント**: メモに追記される。


## Knock 020: docker runオプションをメモ: -d

**目的**: よく使うrunオプションを覚える。

````bash
echo "-d" >> docker_run_options.txt && tail -n 1 docker_run_options.txt
````
**解説**: Dockerはオプションの意味が分かると怖さが減ります。

**確認ポイント**: メモに追記される。


## Knock 021: docker runオプションをメモ: --name

**目的**: よく使うrunオプションを覚える。

````bash
echo "--name" >> docker_run_options.txt && tail -n 1 docker_run_options.txt
````
**解説**: Dockerはオプションの意味が分かると怖さが減ります。

**確認ポイント**: メモに追記される。


## Knock 022: docker runオプションをメモ: -p

**目的**: よく使うrunオプションを覚える。

````bash
echo "-p" >> docker_run_options.txt && tail -n 1 docker_run_options.txt
````
**解説**: Dockerはオプションの意味が分かると怖さが減ります。

**確認ポイント**: メモに追記される。


## Knock 023: docker runオプションをメモ: -v

**目的**: よく使うrunオプションを覚える。

````bash
echo "-v" >> docker_run_options.txt && tail -n 1 docker_run_options.txt
````
**解説**: Dockerはオプションの意味が分かると怖さが減ります。

**確認ポイント**: メモに追記される。


## Knock 024: docker runオプションをメモ: -e

**目的**: よく使うrunオプションを覚える。

````bash
echo "-e" >> docker_run_options.txt && tail -n 1 docker_run_options.txt
````
**解説**: Dockerはオプションの意味が分かると怖さが減ります。

**確認ポイント**: メモに追記される。


## Knock 025: docker runオプションをメモ: --workdir

**目的**: よく使うrunオプションを覚える。

````bash
echo "--workdir" >> docker_run_options.txt && tail -n 1 docker_run_options.txt
````
**解説**: Dockerはオプションの意味が分かると怖さが減ります。

**確認ポイント**: メモに追記される。


## Knock 026: docker runオプションをメモ: --user

**目的**: よく使うrunオプションを覚える。

````bash
echo "--user" >> docker_run_options.txt && tail -n 1 docker_run_options.txt
````
**解説**: Dockerはオプションの意味が分かると怖さが減ります。

**確認ポイント**: メモに追記される。


## Knock 027: docker runオプションをメモ: --pull=missing

**目的**: よく使うrunオプションを覚える。

````bash
echo "--pull=missing" >> docker_run_options.txt && tail -n 1 docker_run_options.txt
````
**解説**: Dockerはオプションの意味が分かると怖さが減ります。

**確認ポイント**: メモに追記される。


## Knock 028: 環境変数を渡す

**目的**: Dockerの便利機能を体験する。

````bash
docker run --rm -e MYVAR=hello ubuntu:22.04 bash -lc "echo $MYVAR"
````
**解説**: 実務では環境変数・マウント・ポートが重要です。

**確認ポイント**: 想定出力を確認する。


## Knock 029: 作業ディレクトリを指定する

**目的**: Dockerの便利機能を体験する。

````bash
docker run --rm --workdir /tmp ubuntu:22.04 pwd
````
**解説**: 実務では環境変数・マウント・ポートが重要です。

**確認ポイント**: 想定出力を確認する。


## Knock 030: ホストディレクトリをマウントする

**目的**: Dockerの便利機能を体験する。

````bash
mkdir -p docker_mount && echo hi > docker_mount/a.txt && docker run --rm -v "$PWD/docker_mount:/work" ubuntu:22.04 cat /work/a.txt
````
**解説**: 実務では環境変数・マウント・ポートが重要です。

**確認ポイント**: 想定出力を確認する。


## Knock 031: ポート指定の形を見る

**目的**: Dockerの便利機能を体験する。

````bash
echo "docker run --rm -p 8080:80 image"
````
**解説**: 実務では環境変数・マウント・ポートが重要です。

**確認ポイント**: 想定出力を確認する。


## Knock 032: 対話シェルを起動する

**目的**: Dockerの便利機能を体験する。

````bash
docker run --rm -it ubuntu:22.04 bash
````
**解説**: 実務では環境変数・マウント・ポートが重要です。

**確認ポイント**: 想定出力を確認する。


## Knock 033: Docker資源確認: docker images

**目的**: Dockerが何を保持しているか見る。

````bash
docker images
````
**解説**: 掃除前に必ず確認します。help系は削除しません。

**確認ポイント**: 一覧またはhelpが表示される。


## Knock 034: Docker資源確認: docker ps -a

**目的**: Dockerが何を保持しているか見る。

````bash
docker ps -a
````
**解説**: 掃除前に必ず確認します。help系は削除しません。

**確認ポイント**: 一覧またはhelpが表示される。


## Knock 035: Docker資源確認: docker volume ls

**目的**: Dockerが何を保持しているか見る。

````bash
docker volume ls
````
**解説**: 掃除前に必ず確認します。help系は削除しません。

**確認ポイント**: 一覧またはhelpが表示される。


## Knock 036: Docker資源確認: docker network ls

**目的**: Dockerが何を保持しているか見る。

````bash
docker network ls
````
**解説**: 掃除前に必ず確認します。help系は削除しません。

**確認ポイント**: 一覧またはhelpが表示される。


## Knock 037: Docker資源確認: docker system df

**目的**: Dockerが何を保持しているか見る。

````bash
docker system df
````
**解説**: 掃除前に必ず確認します。help系は削除しません。

**確認ポイント**: 一覧またはhelpが表示される。


## Knock 038: Docker資源確認: docker builder prune --help

**目的**: Dockerが何を保持しているか見る。

````bash
docker builder prune --help
````
**解説**: 掃除前に必ず確認します。help系は削除しません。

**確認ポイント**: 一覧またはhelpが表示される。


## Knock 039: Docker資源確認: docker container prune --help

**目的**: Dockerが何を保持しているか見る。

````bash
docker container prune --help
````
**解説**: 掃除前に必ず確認します。help系は削除しません。

**確認ポイント**: 一覧またはhelpが表示される。


## Knock 040: Docker資源確認: docker image prune --help

**目的**: Dockerが何を保持しているか見る。

````bash
docker image prune --help
````
**解説**: 掃除前に必ず確認します。help系は削除しません。

**確認ポイント**: 一覧またはhelpが表示される。


## Knock 041: Docker資源確認: docker volume prune --help

**目的**: Dockerが何を保持しているか見る。

````bash
docker volume prune --help
````
**解説**: 掃除前に必ず確認します。help系は削除しません。

**確認ポイント**: 一覧またはhelpが表示される。


## Knock 042: Docker資源確認: docker system prune --help

**目的**: Dockerが何を保持しているか見る。

````bash
docker system prune --help
````
**解説**: 掃除前に必ず確認します。help系は削除しません。

**確認ポイント**: 一覧またはhelpが表示される。


## Knock 043: 一時コンテナでコマンド実行 01

**目的**: 使い捨てコンテナの感覚を掴む。

````bash
docker run --rm ubuntu:22.04 bash -lc "echo knock_01; date"
````
**解説**: `--rm` ならコンテナが残らず練習しやすいです。

**確認ポイント**: knock_01が出る。


## Knock 044: 一時コンテナでコマンド実行 02

**目的**: 使い捨てコンテナの感覚を掴む。

````bash
docker run --rm ubuntu:22.04 bash -lc "echo knock_02; date"
````
**解説**: `--rm` ならコンテナが残らず練習しやすいです。

**確認ポイント**: knock_02が出る。


## Knock 045: 一時コンテナでコマンド実行 03

**目的**: 使い捨てコンテナの感覚を掴む。

````bash
docker run --rm ubuntu:22.04 bash -lc "echo knock_03; date"
````
**解説**: `--rm` ならコンテナが残らず練習しやすいです。

**確認ポイント**: knock_03が出る。


## Knock 046: 一時コンテナでコマンド実行 04

**目的**: 使い捨てコンテナの感覚を掴む。

````bash
docker run --rm ubuntu:22.04 bash -lc "echo knock_04; date"
````
**解説**: `--rm` ならコンテナが残らず練習しやすいです。

**確認ポイント**: knock_04が出る。


## Knock 047: 一時コンテナでコマンド実行 05

**目的**: 使い捨てコンテナの感覚を掴む。

````bash
docker run --rm ubuntu:22.04 bash -lc "echo knock_05; date"
````
**解説**: `--rm` ならコンテナが残らず練習しやすいです。

**確認ポイント**: knock_05が出る。


## Knock 048: 一時コンテナでコマンド実行 06

**目的**: 使い捨てコンテナの感覚を掴む。

````bash
docker run --rm ubuntu:22.04 bash -lc "echo knock_06; date"
````
**解説**: `--rm` ならコンテナが残らず練習しやすいです。

**確認ポイント**: knock_06が出る。


## Knock 049: 一時コンテナでコマンド実行 07

**目的**: 使い捨てコンテナの感覚を掴む。

````bash
docker run --rm ubuntu:22.04 bash -lc "echo knock_07; date"
````
**解説**: `--rm` ならコンテナが残らず練習しやすいです。

**確認ポイント**: knock_07が出る。


## Knock 050: 一時コンテナでコマンド実行 08

**目的**: 使い捨てコンテナの感覚を掴む。

````bash
docker run --rm ubuntu:22.04 bash -lc "echo knock_08; date"
````
**解説**: `--rm` ならコンテナが残らず練習しやすいです。

**確認ポイント**: knock_08が出る。


## Knock 051: 一時コンテナでコマンド実行 09

**目的**: 使い捨てコンテナの感覚を掴む。

````bash
docker run --rm ubuntu:22.04 bash -lc "echo knock_09; date"
````
**解説**: `--rm` ならコンテナが残らず練習しやすいです。

**確認ポイント**: knock_09が出る。


## Knock 052: 一時コンテナでコマンド実行 10

**目的**: 使い捨てコンテナの感覚を掴む。

````bash
docker run --rm ubuntu:22.04 bash -lc "echo knock_10; date"
````
**解説**: `--rm` ならコンテナが残らず練習しやすいです。

**確認ポイント**: knock_10が出る。


## Knock 053: 一時コンテナでコマンド実行 11

**目的**: 使い捨てコンテナの感覚を掴む。

````bash
docker run --rm ubuntu:22.04 bash -lc "echo knock_11; date"
````
**解説**: `--rm` ならコンテナが残らず練習しやすいです。

**確認ポイント**: knock_11が出る。


## Knock 054: 一時コンテナでコマンド実行 12

**目的**: 使い捨てコンテナの感覚を掴む。

````bash
docker run --rm ubuntu:22.04 bash -lc "echo knock_12; date"
````
**解説**: `--rm` ならコンテナが残らず練習しやすいです。

**確認ポイント**: knock_12が出る。


## Knock 055: 一時コンテナでコマンド実行 13

**目的**: 使い捨てコンテナの感覚を掴む。

````bash
docker run --rm ubuntu:22.04 bash -lc "echo knock_13; date"
````
**解説**: `--rm` ならコンテナが残らず練習しやすいです。

**確認ポイント**: knock_13が出る。


## Knock 056: 一時コンテナでコマンド実行 14

**目的**: 使い捨てコンテナの感覚を掴む。

````bash
docker run --rm ubuntu:22.04 bash -lc "echo knock_14; date"
````
**解説**: `--rm` ならコンテナが残らず練習しやすいです。

**確認ポイント**: knock_14が出る。


## Knock 057: 一時コンテナでコマンド実行 15

**目的**: 使い捨てコンテナの感覚を掴む。

````bash
docker run --rm ubuntu:22.04 bash -lc "echo knock_15; date"
````
**解説**: `--rm` ならコンテナが残らず練習しやすいです。

**確認ポイント**: knock_15が出る。


## Knock 058: 一時コンテナでコマンド実行 16

**目的**: 使い捨てコンテナの感覚を掴む。

````bash
docker run --rm ubuntu:22.04 bash -lc "echo knock_16; date"
````
**解説**: `--rm` ならコンテナが残らず練習しやすいです。

**確認ポイント**: knock_16が出る。


## Knock 059: 一時コンテナでコマンド実行 17

**目的**: 使い捨てコンテナの感覚を掴む。

````bash
docker run --rm ubuntu:22.04 bash -lc "echo knock_17; date"
````
**解説**: `--rm` ならコンテナが残らず練習しやすいです。

**確認ポイント**: knock_17が出る。


## Knock 060: 一時コンテナでコマンド実行 18

**目的**: 使い捨てコンテナの感覚を掴む。

````bash
docker run --rm ubuntu:22.04 bash -lc "echo knock_18; date"
````
**解説**: `--rm` ならコンテナが残らず練習しやすいです。

**確認ポイント**: knock_18が出る。


## Knock 061: 一時コンテナでコマンド実行 19

**目的**: 使い捨てコンテナの感覚を掴む。

````bash
docker run --rm ubuntu:22.04 bash -lc "echo knock_19; date"
````
**解説**: `--rm` ならコンテナが残らず練習しやすいです。

**確認ポイント**: knock_19が出る。


## Knock 062: 一時コンテナでコマンド実行 20

**目的**: 使い捨てコンテナの感覚を掴む。

````bash
docker run --rm ubuntu:22.04 bash -lc "echo knock_20; date"
````
**解説**: `--rm` ならコンテナが残らず練習しやすいです。

**確認ポイント**: knock_20が出る。


## Knock 063: 一時コンテナでコマンド実行 21

**目的**: 使い捨てコンテナの感覚を掴む。

````bash
docker run --rm ubuntu:22.04 bash -lc "echo knock_21; date"
````
**解説**: `--rm` ならコンテナが残らず練習しやすいです。

**確認ポイント**: knock_21が出る。


## Knock 064: 一時コンテナでコマンド実行 22

**目的**: 使い捨てコンテナの感覚を掴む。

````bash
docker run --rm ubuntu:22.04 bash -lc "echo knock_22; date"
````
**解説**: `--rm` ならコンテナが残らず練習しやすいです。

**確認ポイント**: knock_22が出る。


## Knock 065: 一時コンテナでコマンド実行 23

**目的**: 使い捨てコンテナの感覚を掴む。

````bash
docker run --rm ubuntu:22.04 bash -lc "echo knock_23; date"
````
**解説**: `--rm` ならコンテナが残らず練習しやすいです。

**確認ポイント**: knock_23が出る。


## Knock 066: 一時コンテナでコマンド実行 24

**目的**: 使い捨てコンテナの感覚を掴む。

````bash
docker run --rm ubuntu:22.04 bash -lc "echo knock_24; date"
````
**解説**: `--rm` ならコンテナが残らず練習しやすいです。

**確認ポイント**: knock_24が出る。


## Knock 067: 一時コンテナでコマンド実行 25

**目的**: 使い捨てコンテナの感覚を掴む。

````bash
docker run --rm ubuntu:22.04 bash -lc "echo knock_25; date"
````
**解説**: `--rm` ならコンテナが残らず練習しやすいです。

**確認ポイント**: knock_25が出る。


## Knock 068: 一時コンテナでコマンド実行 26

**目的**: 使い捨てコンテナの感覚を掴む。

````bash
docker run --rm ubuntu:22.04 bash -lc "echo knock_26; date"
````
**解説**: `--rm` ならコンテナが残らず練習しやすいです。

**確認ポイント**: knock_26が出る。


## Knock 069: 一時コンテナでコマンド実行 27

**目的**: 使い捨てコンテナの感覚を掴む。

````bash
docker run --rm ubuntu:22.04 bash -lc "echo knock_27; date"
````
**解説**: `--rm` ならコンテナが残らず練習しやすいです。

**確認ポイント**: knock_27が出る。


## Knock 070: 一時コンテナでコマンド実行 28

**目的**: 使い捨てコンテナの感覚を掴む。

````bash
docker run --rm ubuntu:22.04 bash -lc "echo knock_28; date"
````
**解説**: `--rm` ならコンテナが残らず練習しやすいです。

**確認ポイント**: knock_28が出る。


## Knock 071: 一時コンテナでコマンド実行 29

**目的**: 使い捨てコンテナの感覚を掴む。

````bash
docker run --rm ubuntu:22.04 bash -lc "echo knock_29; date"
````
**解説**: `--rm` ならコンテナが残らず練習しやすいです。

**確認ポイント**: knock_29が出る。


## Knock 072: 一時コンテナでコマンド実行 30

**目的**: 使い捨てコンテナの感覚を掴む。

````bash
docker run --rm ubuntu:22.04 bash -lc "echo knock_30; date"
````
**解説**: `--rm` ならコンテナが残らず練習しやすいです。

**確認ポイント**: knock_30が出る。


## Knock 073: 総復習チェック 073

**目的**: その領域の練習前後で現在地・時刻・ファイル状態を確認する。

````bash
pwd && date && ls -lah | head
````
**解説**: 初心者ほど、現在地と対象ファイルを確認する癖が重要です。迷ったらまずこの確認セットに戻ります。

**確認ポイント**: 現在地、日時、ファイル一覧の先頭が表示される。


## Knock 074: 総復習チェック 074

**目的**: その領域の練習前後で現在地・時刻・ファイル状態を確認する。

````bash
pwd && date && ls -lah | head
````
**解説**: 初心者ほど、現在地と対象ファイルを確認する癖が重要です。迷ったらまずこの確認セットに戻ります。

**確認ポイント**: 現在地、日時、ファイル一覧の先頭が表示される。


## Knock 075: 総復習チェック 075

**目的**: その領域の練習前後で現在地・時刻・ファイル状態を確認する。

````bash
pwd && date && ls -lah | head
````
**解説**: 初心者ほど、現在地と対象ファイルを確認する癖が重要です。迷ったらまずこの確認セットに戻ります。

**確認ポイント**: 現在地、日時、ファイル一覧の先頭が表示される。


## Knock 076: 総復習チェック 076

**目的**: その領域の練習前後で現在地・時刻・ファイル状態を確認する。

````bash
pwd && date && ls -lah | head
````
**解説**: 初心者ほど、現在地と対象ファイルを確認する癖が重要です。迷ったらまずこの確認セットに戻ります。

**確認ポイント**: 現在地、日時、ファイル一覧の先頭が表示される。


## Knock 077: 総復習チェック 077

**目的**: その領域の練習前後で現在地・時刻・ファイル状態を確認する。

````bash
pwd && date && ls -lah | head
````
**解説**: 初心者ほど、現在地と対象ファイルを確認する癖が重要です。迷ったらまずこの確認セットに戻ります。

**確認ポイント**: 現在地、日時、ファイル一覧の先頭が表示される。


## Knock 078: 総復習チェック 078

**目的**: その領域の練習前後で現在地・時刻・ファイル状態を確認する。

````bash
pwd && date && ls -lah | head
````
**解説**: 初心者ほど、現在地と対象ファイルを確認する癖が重要です。迷ったらまずこの確認セットに戻ります。

**確認ポイント**: 現在地、日時、ファイル一覧の先頭が表示される。


## Knock 079: 総復習チェック 079

**目的**: その領域の練習前後で現在地・時刻・ファイル状態を確認する。

````bash
pwd && date && ls -lah | head
````
**解説**: 初心者ほど、現在地と対象ファイルを確認する癖が重要です。迷ったらまずこの確認セットに戻ります。

**確認ポイント**: 現在地、日時、ファイル一覧の先頭が表示される。


## Knock 080: 総復習チェック 080

**目的**: その領域の練習前後で現在地・時刻・ファイル状態を確認する。

````bash
pwd && date && ls -lah | head
````
**解説**: 初心者ほど、現在地と対象ファイルを確認する癖が重要です。迷ったらまずこの確認セットに戻ります。

**確認ポイント**: 現在地、日時、ファイル一覧の先頭が表示される。


## Knock 081: 総復習チェック 081

**目的**: その領域の練習前後で現在地・時刻・ファイル状態を確認する。

````bash
pwd && date && ls -lah | head
````
**解説**: 初心者ほど、現在地と対象ファイルを確認する癖が重要です。迷ったらまずこの確認セットに戻ります。

**確認ポイント**: 現在地、日時、ファイル一覧の先頭が表示される。


## Knock 082: 総復習チェック 082

**目的**: その領域の練習前後で現在地・時刻・ファイル状態を確認する。

````bash
pwd && date && ls -lah | head
````
**解説**: 初心者ほど、現在地と対象ファイルを確認する癖が重要です。迷ったらまずこの確認セットに戻ります。

**確認ポイント**: 現在地、日時、ファイル一覧の先頭が表示される。


## Knock 083: 総復習チェック 083

**目的**: その領域の練習前後で現在地・時刻・ファイル状態を確認する。

````bash
pwd && date && ls -lah | head
````
**解説**: 初心者ほど、現在地と対象ファイルを確認する癖が重要です。迷ったらまずこの確認セットに戻ります。

**確認ポイント**: 現在地、日時、ファイル一覧の先頭が表示される。


## Knock 084: 総復習チェック 084

**目的**: その領域の練習前後で現在地・時刻・ファイル状態を確認する。

````bash
pwd && date && ls -lah | head
````
**解説**: 初心者ほど、現在地と対象ファイルを確認する癖が重要です。迷ったらまずこの確認セットに戻ります。

**確認ポイント**: 現在地、日時、ファイル一覧の先頭が表示される。


## Knock 085: 総復習チェック 085

**目的**: その領域の練習前後で現在地・時刻・ファイル状態を確認する。

````bash
pwd && date && ls -lah | head
````
**解説**: 初心者ほど、現在地と対象ファイルを確認する癖が重要です。迷ったらまずこの確認セットに戻ります。

**確認ポイント**: 現在地、日時、ファイル一覧の先頭が表示される。


## Knock 086: 総復習チェック 086

**目的**: その領域の練習前後で現在地・時刻・ファイル状態を確認する。

````bash
pwd && date && ls -lah | head
````
**解説**: 初心者ほど、現在地と対象ファイルを確認する癖が重要です。迷ったらまずこの確認セットに戻ります。

**確認ポイント**: 現在地、日時、ファイル一覧の先頭が表示される。


## Knock 087: 総復習チェック 087

**目的**: その領域の練習前後で現在地・時刻・ファイル状態を確認する。

````bash
pwd && date && ls -lah | head
````
**解説**: 初心者ほど、現在地と対象ファイルを確認する癖が重要です。迷ったらまずこの確認セットに戻ります。

**確認ポイント**: 現在地、日時、ファイル一覧の先頭が表示される。


## Knock 088: 総復習チェック 088

**目的**: その領域の練習前後で現在地・時刻・ファイル状態を確認する。

````bash
pwd && date && ls -lah | head
````
**解説**: 初心者ほど、現在地と対象ファイルを確認する癖が重要です。迷ったらまずこの確認セットに戻ります。

**確認ポイント**: 現在地、日時、ファイル一覧の先頭が表示される。


## Knock 089: 総復習チェック 089

**目的**: その領域の練習前後で現在地・時刻・ファイル状態を確認する。

````bash
pwd && date && ls -lah | head
````
**解説**: 初心者ほど、現在地と対象ファイルを確認する癖が重要です。迷ったらまずこの確認セットに戻ります。

**確認ポイント**: 現在地、日時、ファイル一覧の先頭が表示される。


## Knock 090: 総復習チェック 090

**目的**: その領域の練習前後で現在地・時刻・ファイル状態を確認する。

````bash
pwd && date && ls -lah | head
````
**解説**: 初心者ほど、現在地と対象ファイルを確認する癖が重要です。迷ったらまずこの確認セットに戻ります。

**確認ポイント**: 現在地、日時、ファイル一覧の先頭が表示される。


## Knock 091: 総復習チェック 091

**目的**: その領域の練習前後で現在地・時刻・ファイル状態を確認する。

````bash
pwd && date && ls -lah | head
````
**解説**: 初心者ほど、現在地と対象ファイルを確認する癖が重要です。迷ったらまずこの確認セットに戻ります。

**確認ポイント**: 現在地、日時、ファイル一覧の先頭が表示される。


## Knock 092: 総復習チェック 092

**目的**: その領域の練習前後で現在地・時刻・ファイル状態を確認する。

````bash
pwd && date && ls -lah | head
````
**解説**: 初心者ほど、現在地と対象ファイルを確認する癖が重要です。迷ったらまずこの確認セットに戻ります。

**確認ポイント**: 現在地、日時、ファイル一覧の先頭が表示される。


## Knock 093: 総復習チェック 093

**目的**: その領域の練習前後で現在地・時刻・ファイル状態を確認する。

````bash
pwd && date && ls -lah | head
````
**解説**: 初心者ほど、現在地と対象ファイルを確認する癖が重要です。迷ったらまずこの確認セットに戻ります。

**確認ポイント**: 現在地、日時、ファイル一覧の先頭が表示される。


## Knock 094: 総復習チェック 094

**目的**: その領域の練習前後で現在地・時刻・ファイル状態を確認する。

````bash
pwd && date && ls -lah | head
````
**解説**: 初心者ほど、現在地と対象ファイルを確認する癖が重要です。迷ったらまずこの確認セットに戻ります。

**確認ポイント**: 現在地、日時、ファイル一覧の先頭が表示される。


## Knock 095: 総復習チェック 095

**目的**: その領域の練習前後で現在地・時刻・ファイル状態を確認する。

````bash
pwd && date && ls -lah | head
````
**解説**: 初心者ほど、現在地と対象ファイルを確認する癖が重要です。迷ったらまずこの確認セットに戻ります。

**確認ポイント**: 現在地、日時、ファイル一覧の先頭が表示される。


## Knock 096: 総復習チェック 096

**目的**: その領域の練習前後で現在地・時刻・ファイル状態を確認する。

````bash
pwd && date && ls -lah | head
````
**解説**: 初心者ほど、現在地と対象ファイルを確認する癖が重要です。迷ったらまずこの確認セットに戻ります。

**確認ポイント**: 現在地、日時、ファイル一覧の先頭が表示される。


## Knock 097: 総復習チェック 097

**目的**: その領域の練習前後で現在地・時刻・ファイル状態を確認する。

````bash
pwd && date && ls -lah | head
````
**解説**: 初心者ほど、現在地と対象ファイルを確認する癖が重要です。迷ったらまずこの確認セットに戻ります。

**確認ポイント**: 現在地、日時、ファイル一覧の先頭が表示される。


## Knock 098: 総復習チェック 098

**目的**: その領域の練習前後で現在地・時刻・ファイル状態を確認する。

````bash
pwd && date && ls -lah | head
````
**解説**: 初心者ほど、現在地と対象ファイルを確認する癖が重要です。迷ったらまずこの確認セットに戻ります。

**確認ポイント**: 現在地、日時、ファイル一覧の先頭が表示される。


## Knock 099: 総復習チェック 099

**目的**: その領域の練習前後で現在地・時刻・ファイル状態を確認する。

````bash
pwd && date && ls -lah | head
````
**解説**: 初心者ほど、現在地と対象ファイルを確認する癖が重要です。迷ったらまずこの確認セットに戻ります。

**確認ポイント**: 現在地、日時、ファイル一覧の先頭が表示される。


## Knock 100: 総復習チェック 100

**目的**: その領域の練習前後で現在地・時刻・ファイル状態を確認する。

````bash
pwd && date && ls -lah | head
````
**解説**: 初心者ほど、現在地と対象ファイルを確認する癖が重要です。迷ったらまずこの確認セットに戻ります。

**確認ポイント**: 現在地、日時、ファイル一覧の先頭が表示される。

